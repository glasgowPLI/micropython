
firmware("micropython")
    add_deps("freestanding")
    add_deps("string", "stdio", "soft_fp")
    add_deps("mp_vm", "mp_hal")
    add_deps("main")
    on_load(function(target)
        target:values_set("board", "$(board)")
        target:values_set("threads", {
            {
                compartment = "main",
                priority = 1,
                entry_point = "entry",
                stack_size = 0x1f00,
                trusted_stack_frames = 4
            }
        }, {expand = false})
    end)

